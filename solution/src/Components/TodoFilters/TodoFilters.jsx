import React from 'react';
import { StyledTodoFilters } from './StyledTodoFilters';

const TodoFilters = ({ children }) => {
  return <StyledTodoFilters>{children}</StyledTodoFilters>;
};

export { TodoFilters };
